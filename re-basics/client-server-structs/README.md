# Client/Server Struct Packet

Reverse the provided stripped 32-bit Linux client binary.

The server sends a seed. The client builds a packet with six integer values.

Run the challenge and enter the six values:

```bash
/challenge/run